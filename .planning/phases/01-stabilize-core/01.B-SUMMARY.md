---
phase: 01-stabilize-core
plan: 01.B
subsystem: analysis
tags: [refactor, registry, constants, dfm]
requires: [01.A]
provides: ["src.analysis.constants", "registry-only dispatch in routes.py"]
affects:
  - backend/src/analysis/constants.py
  - backend/src/analysis/additive_analyzer.py
  - backend/src/analysis/cnc_analyzer.py
  - backend/src/analysis/molding_analyzer.py
  - backend/src/analysis/casting_analyzer.py
  - backend/src/analysis/sheet_metal_analyzer.py
  - backend/src/analysis/processes/checks.py
  - backend/src/api/routes.py
  - backend/tests/test_registry_coverage.py
tech-stack:
  added: []
  patterns: ["single-source-of-truth constants module", "registry-only dispatch"]
key-files:
  created:
    - backend/src/analysis/constants.py
    - backend/tests/test_registry_coverage.py
  modified:
    - backend/src/analysis/additive_analyzer.py
    - backend/src/analysis/cnc_analyzer.py
    - backend/src/analysis/molding_analyzer.py
    - backend/src/analysis/casting_analyzer.py
    - backend/src/analysis/sheet_metal_analyzer.py
    - backend/src/analysis/processes/checks.py
    - backend/src/api/routes.py
decisions:
  - "Moved BEND_RADIUS_MULTIPLIER, MIN_DRAFT_ANGLE, WALL_THICKNESS_RANGE, MIN_FILLET_RADIUS to constants.py (beyond plan's explicit list) to avoid leaving inline dicts that would re-introduce the divergence risk"
metrics:
  completed: 2026-04-15
  tasks: 4
  duration: "~15 min"
---

# Phase 01 Plan 01.B: Registry-only Dispatch + Centralized Constants — Summary

Completed CORE-03 (registry-only dispatch) and CORE-04 (single-source DFM thresholds): created `backend/src/analysis/constants.py` holding every manufacturing threshold; migrated all five legacy analyzer files + `processes/checks.py` to import from it; deleted the `PROCESS_ANALYZERS` dict and legacy fallback `else:` branch from `routes.py`.

## Constants Extracted

| Symbol | Source File | Destination Section |
| --- | --- | --- |
| `MIN_WALL_THICKNESS` | additive_analyzer.py | Additive thresholds |
| `SUPPORT_ANGLE_THRESHOLD` | additive_analyzer.py | Additive thresholds |
| `MIN_FEATURE_SIZE` | additive_analyzer.py | Additive thresholds |
| `BUILD_VOLUMES` | additive_analyzer.py (local to `check_build_volume`) | Additive build volumes |
| `STANDARD_TOOL_DIAMETERS` | cnc_analyzer.py | CNC thresholds |
| `MAX_WORKPIECE` | cnc_analyzer.py | CNC thresholds |
| `MAX_POCKET_DEPTH_RATIO` | cnc_analyzer.py | CNC thresholds |
| `MIN_DRAFT_ANGLE` | molding_analyzer.py | Molding / casting |
| `WALL_THICKNESS_RANGE` | molding_analyzer.py | Molding / casting |
| `MIN_FILLET_RADIUS` | casting_analyzer.py | Molding / casting |
| `BEND_RADIUS_MULTIPLIER` | sheet_metal_analyzer.py | Sheet metal |
| `STANDARD_GAUGES` | sheet_metal_analyzer.py + processes/checks.py (duplicated!) | Sheet metal |

Notable: `STANDARD_GAUGES` was defined in two places. The plan only called out the `checks.py` copy; both were replaced with the same imported symbol.

## Registry Coverage

`get_analyzer()` returned a non-None entry for all 21 `ProcessType` values before the legacy fallback was removed — no new wrappers needed.

## Commits

| Task | Commit | Message |
| --- | --- | --- |
| B1 | 1435547 | feat(01.B): centralize manufacturing constants in constants.py |
| B2 | 87b6467 | refactor(01.B): import manufacturing constants from constants.py |
| B3 | ad2f237 | refactor(01.B): remove PROCESS_ANALYZERS dict, use registry-only dispatch |
| B4 | e2298ee | test(01.B): add registry coverage and constants identity regression tests |

## Deviations from Plan

**1. [Rule 2 - Completeness] Extended constant extraction beyond the explicit list.** The plan required extracting `MIN_WALL_THICKNESS`, `SUPPORT_ANGLE_THRESHOLD`, `MIN_FEATURE_SIZE`, `STANDARD_TOOL_DIAMETERS`, `MAX_WORKPIECE`, `MAX_POCKET_DEPTH_RATIO`, `STANDARD_GAUGES`, `BUILD_VOLUMES`. The plan text also said "If molding_analyzer.py or casting_analyzer.py have similar constants blocks, extract those too" — they did, so I additionally extracted `MIN_DRAFT_ANGLE`, `WALL_THICKNESS_RANGE`, `MIN_FILLET_RADIUS`, `BEND_RADIUS_MULTIPLIER`. Leaving those inline would re-introduce the divergence risk this plan exists to eliminate.

## Test Results

- `pytest backend/tests/` → 93 passed, 3 failed, 1 skipped.
- **All 3 failures are pre-existing** (same results before any edits in this plan): `test_every_analyzer_runs_on_thin_plate`, `test_cube_wall_thickness_is_10mm`, `test_thin_plate_minimum_thickness`. Root cause is ray-casting returning empty `wall_thickness` arrays on procedural `trimesh.creation.box` meshes — unrelated to dispatch or constants. Deferred to whichever plan owns the ray-casting/context pipeline.
- **New test file** `test_registry_coverage.py` → all 3 tests pass (registry coverage, constants identity via `is`, cube smoke dispatch).

## Verification (must_haves)

- `grep -Rn PROCESS_ANALYZERS backend/src/` → 0 hits ✓
- `grep -Rn "^MIN_WALL_THICKNESS = \\{" backend/src/analysis/` → 0 hits (only constants.py, and it's typed `dict[ProcessType, float] =`) ✓
- Registry covers all 21 ProcessType values ✓
- `test_constants_module_is_single_source` asserts `aa.MIN_WALL_THICKNESS is c.MIN_WALL_THICKNESS` ✓

## Self-Check: PASSED

- FOUND: backend/src/analysis/constants.py
- FOUND: backend/tests/test_registry_coverage.py
- FOUND commits: 1435547, 87b6467, ad2f237, e2298ee
