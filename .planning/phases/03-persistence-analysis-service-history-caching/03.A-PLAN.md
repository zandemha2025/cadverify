---
phase: 03-persistence-analysis-service-history-caching
plan: 03.A
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt                                   # add python-ulid
  - backend/src/__init__.py                                    # add __version__ = "0.3.0"
  - backend/src/db/__init__.py                                 # NEW package
  - backend/src/db/engine.py                                   # NEW — async engine + session factory + get_db_session dependency
  - backend/src/db/models.py                                   # NEW — ORM mapped classes for all 5 tables
  - backend/alembic/env.py                                     # promote target_metadata to Base.metadata
  - backend/alembic/versions/0002_create_analyses_jobs_usage_events.py  # NEW migration
  - backend/src/auth/models.py                                 # redirect engine/session to db/engine.py
autonomous: true
requirements: [PERS-01, PERS-02]
must_haves:
  truths:
    - "Running `alembic upgrade head` on a fresh DB creates all 5 tables: users, api_keys, analyses, jobs, usage_events"
    - "Running `alembic downgrade -1` removes only the Phase 3 tables (analyses, jobs, usage_events); users + api_keys survive"
    - "`analyses` table has UNIQUE index on (user_id, mesh_hash, process_set_hash, analysis_version)"
    - "`analyses` table has index on (user_id, created_at DESC) for history pagination"
    - "`analyses.ulid` column is TEXT UNIQUE NOT NULL"
    - "`analyses.result_json` column is JSONB NOT NULL"
    - "`jobs` and `usage_events` tables match the schemas in 03-CONTEXT.md D-17 and D-18"
    - "ORM Base.metadata is wired into alembic env.py target_metadata"
    - "get_db_session() yields a request-scoped AsyncSession with commit-on-success and rollback-on-error"
    - "Phase 2's auth queries still work after engine promotion (auth/models.py imports from db/engine.py)"
    - "backend/src/__init__.py contains __version__ = \"0.3.0\""
  artifacts:
    - path: backend/src/db/engine.py
      provides: "Async engine singleton, session factory, get_db_session FastAPI dependency"
      contains: "async def get_db_session"
    - path: backend/src/db/models.py
      provides: "ORM mapped classes: User, ApiKey, Analysis, Job, UsageEvent"
      contains: "class Analysis(Base)"
    - path: backend/alembic/versions/0002_create_analyses_jobs_usage_events.py
      provides: "Migration creating analyses, jobs, usage_events tables with indexes"
      contains: "op.create_table(\"analyses\""
  key_links:
    - from: backend/src/db/engine.py::Base
      to: backend/src/db/models.py::Analysis
      via: "inheritance — Analysis(Base)"
      pattern: "class Analysis\\(Base\\)"
    - from: backend/alembic/env.py::target_metadata
      to: backend/src/db/models.py::Base.metadata
      via: "import — target_metadata = Base.metadata"
      pattern: "target_metadata = Base.metadata"
    - from: backend/src/auth/models.py::_engine
      to: backend/src/db/engine.py::get_engine
      via: "import redirect"
      pattern: "from src.db.engine import get_engine"
threat_model:
  threats:
    - id: T-03A-01
      severity: medium
      description: "SQL injection via raw text() queries in auth/models.py"
      mitigation: "All queries use parameterized :name bindings (existing pattern). ORM models use mapped columns, not string interpolation."
    - id: T-03A-02
      severity: low
      description: "Migration could drop data if down_revision is wrong"
      mitigation: "CI gate runs upgrade + downgrade -1 + upgrade on every PR. Pure additive migration — no ALTER TABLE on existing tables."
    - id: T-03A-03
      severity: medium
      description: "Database connection leak if session not properly closed"
      mitigation: "get_db_session uses async context manager with try/except/finally pattern. expire_on_commit=False prevents lazy-load post-commit."
---

<objective>
Deliver PERS-01 (Postgres schema via Alembic for analyses, jobs, usage_events tables)
and PERS-02 (expand-migrate-contract discipline with CI migration gate). Establish the
ORM foundation that Plans 03.B, 03.C, and 03.D depend on.

This is a Wave 1 plan with no dependencies — it creates the database layer that all
subsequent Phase 3 plans consume.

Output:
- `backend/src/db/` package with engine.py (async engine + session factory + FastAPI
  dependency) and models.py (ORM mapped classes for all 5 tables)
- Alembic migration 0002 creating analyses, jobs, usage_events tables with proper
  indexes and foreign keys
- Alembic env.py promoted to use ORM metadata for autogenerate
- Phase 2 auth/models.py redirected to use db/engine.py (backward-compatible)
- `__version__ = "0.3.0"` in backend/src/__init__.py
</objective>

<tasks>

<task id="03.A.1" type="execute">
<title>Add python-ulid dependency and __version__</title>
<read_first>
- backend/requirements.txt
- backend/src/__init__.py
</read_first>
<files>
- backend/requirements.txt
- backend/src/__init__.py
</files>
<action>
1. Add `python-ulid>=3.0.0,<4.0.0` to backend/requirements.txt (after the sqlalchemy line).
2. Add `__version__ = "0.3.0"` to backend/src/__init__.py. This version string is used as
   the `analysis_version` component of the dedup cache key.
</action>
<acceptance_criteria>
- `grep 'python-ulid' backend/requirements.txt` matches `python-ulid>=3.0.0`
- `grep '__version__' backend/src/__init__.py` matches `__version__ = "0.3.0"`
</acceptance_criteria>
</task>

<task id="03.A.2" type="execute">
<title>Create db/ package with engine.py</title>
<read_first>
- backend/src/auth/models.py
- backend/src/db/__init__.py (will not exist yet — create it)
</read_first>
<files>
- backend/src/db/__init__.py
- backend/src/db/engine.py
</files>
<action>
1. Create `backend/src/db/__init__.py` (empty package init).

2. Create `backend/src/db/engine.py` with:
   - `from sqlalchemy.orm import DeclarativeBase` -> `class Base(DeclarativeBase): pass`
   - Lazy singleton `get_engine()` reading `os.environ["DATABASE_URL"]`, with `pool_pre_ping=True`, `pool_size=5`
   - Lazy singleton `get_session_factory()` returning `async_sessionmaker[AsyncSession]` with `expire_on_commit=False`
   - `async def get_db_session()` generator — FastAPI Depends-compatible:
     ```python
     async def get_db_session():
         async with get_session_factory()() as session:
             try:
                 yield session
                 await session.commit()
             except Exception:
                 await session.rollback()
                 raise
     ```
   - `async def dispose_engine()` for shutdown cleanup
</action>
<acceptance_criteria>
- `test -f backend/src/db/__init__.py` exits 0
- `test -f backend/src/db/engine.py` exits 0
- `grep 'class Base(DeclarativeBase)' backend/src/db/engine.py` matches
- `grep 'async def get_db_session' backend/src/db/engine.py` matches
- `grep 'pool_pre_ping=True' backend/src/db/engine.py` matches
- `grep 'expire_on_commit=False' backend/src/db/engine.py` matches
</acceptance_criteria>
</task>

<task id="03.A.3" type="execute">
<title>Create ORM models for all 5 tables</title>
<read_first>
- backend/src/db/engine.py
- backend/alembic/versions/0001_create_users_api_keys.py
- .planning/phases/03-persistence-analysis-service-history-caching/03-CONTEXT.md (D-15, D-17, D-18 schemas)
</read_first>
<files>
- backend/src/db/models.py
</files>
<action>
Create `backend/src/db/models.py` with ORM mapped classes:

1. **User** (`__tablename__ = "users"`):
   - `id: Mapped[int]` BigInteger PK
   - `email: Mapped[str]` Text UNIQUE NOT NULL
   - `email_lower: Mapped[str]` Text UNIQUE NOT NULL
   - `google_sub: Mapped[str | None]` Text UNIQUE nullable
   - `created_at: Mapped[datetime]` TIMESTAMP(timezone=True) server_default=func.now()
   - `disposable_flag: Mapped[bool]` Boolean server_default="false"
   - Relationships: `api_keys`, `analyses`

2. **ApiKey** (`__tablename__ = "api_keys"`):
   - Match all columns from 0001 migration exactly
   - `user_id` FK to users.id ON DELETE CASCADE
   - Relationships: `user`

3. **Analysis** (`__tablename__ = "analyses"`):
   Per D-15:
   - `id: Mapped[int]` BigInteger PK (GENERATED ALWAYS AS IDENTITY)
   - `ulid: Mapped[str]` Text UNIQUE NOT NULL
   - `user_id: Mapped[int]` FK(users.id ON DELETE CASCADE) NOT NULL
   - `api_key_id: Mapped[int | None]` FK(api_keys.id ON DELETE SET NULL)
   - `mesh_hash: Mapped[str]` Text NOT NULL
   - `process_set_hash: Mapped[str]` Text NOT NULL
   - `analysis_version: Mapped[str]` Text NOT NULL
   - `filename: Mapped[str]` Text NOT NULL
   - `file_type: Mapped[str]` Text NOT NULL
   - `file_size_bytes: Mapped[int]` BigInteger NOT NULL
   - `result_json: Mapped[dict]` JSONB NOT NULL
   - `verdict: Mapped[str]` Text NOT NULL
   - `face_count: Mapped[int]` Integer NOT NULL
   - `duration_ms: Mapped[float]` Float NOT NULL
   - `is_public: Mapped[bool]` Boolean default=False NOT NULL
   - `share_short_id: Mapped[str | None]` Text UNIQUE nullable
   - `created_at: Mapped[datetime]` TIMESTAMP(timezone=True) server_default=func.now()
   - Table args with indexes:
     - `Index("ix_analyses_user_created", "user_id", "created_at", postgresql_using="btree")`  (created_at DESC handled in query)
     - `UniqueConstraint("user_id", "mesh_hash", "process_set_hash", "analysis_version", name="uq_analyses_dedup")`
     - `Index("ix_analyses_share", "share_short_id", unique=True, postgresql_where=text("share_short_id IS NOT NULL"))`

4. **Job** (`__tablename__ = "jobs"`):
   Per D-17:
   - `id, ulid, user_id, analysis_id, job_type, status, params_json, result_json, created_at, started_at, completed_at`
   - All columns as specified in D-17

5. **UsageEvent** (`__tablename__ = "usage_events"`):
   Per D-18:
   - `id, user_id, api_key_id, event_type, analysis_id, mesh_hash, duration_ms, face_count, created_at`
   - Indexes: `(user_id, created_at DESC)`, `(api_key_id, created_at DESC)`

Import `from ulid import ULID` and use `default=lambda: str(ULID())` for ulid columns.
</action>
<acceptance_criteria>
- `grep 'class User(Base)' backend/src/db/models.py` matches
- `grep 'class ApiKey(Base)' backend/src/db/models.py` matches
- `grep 'class Analysis(Base)' backend/src/db/models.py` matches
- `grep 'class Job(Base)' backend/src/db/models.py` matches
- `grep 'class UsageEvent(Base)' backend/src/db/models.py` matches
- `grep 'uq_analyses_dedup' backend/src/db/models.py` matches
- `grep 'ix_analyses_user_created' backend/src/db/models.py` matches
- `grep 'JSONB' backend/src/db/models.py` matches
- `grep 'from ulid import ULID' backend/src/db/models.py` matches
</acceptance_criteria>
</task>

<task id="03.A.4" type="execute">
<title>Create Alembic migration 0002</title>
<read_first>
- backend/alembic/versions/0001_create_users_api_keys.py
- backend/src/db/models.py
- .planning/phases/03-persistence-analysis-service-history-caching/03-CONTEXT.md (D-04, D-06)
</read_first>
<files>
- backend/alembic/versions/0002_create_analyses_jobs_usage_events.py
</files>
<action>
Create `backend/alembic/versions/0002_create_analyses_jobs_usage_events.py`:

```python
revision = "0002_create_analyses_jobs_usage_events"
down_revision = "0001_create_users_api_keys"
```

`upgrade()`:
1. Set `statement_timeout = '5s'` via `op.execute("SET statement_timeout = '5000'")`
2. Create `analyses` table with all columns from D-15:
   - id BIGINT PRIMARY KEY (autoincrement)
   - ulid TEXT UNIQUE NOT NULL
   - user_id BIGINT NOT NULL FK(users.id ON DELETE CASCADE)
   - api_key_id BIGINT NULL FK(api_keys.id ON DELETE SET NULL)
   - mesh_hash TEXT NOT NULL
   - process_set_hash TEXT NOT NULL
   - analysis_version TEXT NOT NULL
   - filename TEXT NOT NULL
   - file_type TEXT NOT NULL
   - file_size_bytes BIGINT NOT NULL
   - result_json JSONB NOT NULL
   - verdict TEXT NOT NULL
   - face_count INTEGER NOT NULL
   - duration_ms REAL NOT NULL
   - is_public BOOLEAN NOT NULL DEFAULT false
   - share_short_id TEXT UNIQUE NULL
   - created_at TIMESTAMPTZ NOT NULL DEFAULT now()
3. Create indexes on analyses:
   - `ix_analyses_user_created` on (user_id, created_at DESC)
   - `uq_analyses_dedup` UNIQUE on (user_id, mesh_hash, process_set_hash, analysis_version)
   - `ix_analyses_share` UNIQUE on (share_short_id) WHERE share_short_id IS NOT NULL
4. Create `jobs` table with all columns from D-17
5. Create `usage_events` table with all columns from D-18
6. Create indexes on usage_events:
   - `ix_usage_events_user_created` on (user_id, created_at DESC)
   - `ix_usage_events_apikey_created` on (api_key_id, created_at DESC)

`downgrade()`:
1. Drop indexes in reverse order
2. Drop tables: usage_events, jobs, analyses (reverse order respecting FKs)
</action>
<acceptance_criteria>
- `grep 'down_revision = "0001_create_users_api_keys"' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'op.create_table("analyses"' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'op.create_table("jobs"' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'op.create_table("usage_events"' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'statement_timeout' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'uq_analyses_dedup' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
- `grep 'op.drop_table("analyses")' backend/alembic/versions/0002_create_analyses_jobs_usage_events.py` matches
</acceptance_criteria>
</task>

<task id="03.A.5" type="execute">
<title>Promote Alembic env.py and redirect auth/models.py</title>
<read_first>
- backend/alembic/env.py
- backend/src/auth/models.py
- backend/src/db/engine.py
</read_first>
<files>
- backend/alembic/env.py
- backend/src/auth/models.py
</files>
<action>
1. **backend/alembic/env.py:** Replace `target_metadata = None` with:
   ```python
   from src.db.models import Base  # noqa: E402 — must import after path setup
   # Import all model modules so they register with Base.metadata
   import src.db.models  # noqa: F401
   target_metadata = Base.metadata
   ```

2. **backend/src/auth/models.py:** Replace the `_engine()` and `_session()` singletons with
   imports from `db/engine.py`:
   ```python
   from src.db.engine import get_engine as _engine, get_session_factory
   
   def _session():
       return get_session_factory()
   ```
   Keep all existing query functions (`upsert_user`, `create_api_key`, `lookup_api_key`,
   `touch_last_used`) unchanged — they use `_session()()` which still works because
   `get_session_factory()` returns the same `async_sessionmaker` type.
   Remove the direct `create_async_engine` / `async_sessionmaker` imports that are no
   longer needed.
</action>
<acceptance_criteria>
- `grep 'target_metadata = Base.metadata' backend/alembic/env.py` matches
- `grep 'from src.db.models import Base' backend/alembic/env.py` matches (or equivalent import)
- `grep 'from src.db.engine import' backend/src/auth/models.py` matches
- `grep 'def _session' backend/src/auth/models.py` matches (wrapper still exists)
- `grep 'upsert_user' backend/src/auth/models.py` matches (existing functions preserved)
- `grep 'create_async_engine' backend/src/auth/models.py` does NOT match (removed — now in db/engine.py)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `python -c "from src.db.models import Base, User, ApiKey, Analysis, Job, UsageEvent; print('ORM OK')"` prints "ORM OK"
2. `python -c "from src.db.engine import get_engine, get_session_factory, get_db_session; print('Engine OK')"` prints "Engine OK"
3. `python -c "from src import __version__; assert __version__ == '0.3.0'; print('Version OK')"` prints "Version OK"
4. `alembic upgrade head` completes without errors on a fresh DB
5. `alembic downgrade -1` removes Phase 3 tables; Phase 2 tables remain
6. `alembic upgrade head` re-applies cleanly (idempotency)
7. Auth queries (`upsert_user`, `lookup_api_key`) still work after engine redirect
8. `grep -r 'create_async_engine' backend/src/auth/` returns no matches (engine creation centralized in db/engine.py)
</verification>

<success_criteria>
- All 5 ORM model classes defined and importable
- Migration 0002 creates analyses, jobs, usage_events with correct columns and indexes
- Dedup unique constraint exists on analyses
- Alembic env.py uses real ORM metadata
- Phase 2 auth functions work unchanged
- __version__ = "0.3.0" set in backend/src/__init__.py
</success_criteria>
