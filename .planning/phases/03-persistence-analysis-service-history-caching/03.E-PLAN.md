---
phase: 03-persistence-analysis-service-history-caching
plan: 03.E
type: execute
wave: 3
depends_on: [03.B, 03.C, 03.D]
files_modified:
  - backend/tests/test_analysis_service.py                     # NEW — unit tests for service layer
  - backend/tests/test_history_api.py                          # NEW — integration tests for history endpoints
  - backend/tests/test_dedup.py                                # NEW — dedup verification + race condition tests
  - backend/tests/test_migration_0002.py                       # NEW — migration up/down smoke test
  - backend/tests/conftest.py                                  # modify — add DB fixtures
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05, PERS-06, PERS-07, PERS-08]
must_haves:
  truths:
    - "test_analysis_service.py tests: compute_mesh_hash determinism, compute_process_set_hash sort-invariance, run_analysis fresh path, run_analysis cache hit path, usage_events written"
    - "test_dedup.py tests: same file + same processes = cache hit, same file + different processes = cache miss, different file + same processes = cache miss, different user + same file = separate row (per-user dedup D-13)"
    - "test_history_api.py tests: empty list, paginated list, cursor traversal, verdict filter, detail endpoint, 404 for wrong user, 404 for nonexistent"
    - "test_migration_0002.py tests: upgrade creates tables, downgrade removes them, re-upgrade works"
    - "conftest.py provides async DB session fixture using test database"
    - "All tests pass with `pytest backend/tests/test_analysis_service.py backend/tests/test_history_api.py backend/tests/test_dedup.py backend/tests/test_migration_0002.py`"
  artifacts:
    - path: backend/tests/test_analysis_service.py
      provides: "Unit tests for analysis_service hash functions and pipeline orchestration"
      contains: "def test_compute_mesh_hash"
    - path: backend/tests/test_dedup.py
      provides: "Integration tests for dedup behavior"
      contains: "def test_same_file_same_processes_cache_hit"
    - path: backend/tests/test_history_api.py
      provides: "Integration tests for history API endpoints"
      contains: "def test_list_analyses_pagination"
    - path: backend/tests/test_migration_0002.py
      provides: "Migration smoke tests"
      contains: "def test_upgrade_creates_tables"
threat_model:
  threats:
    - id: T-03E-01
      severity: low
      description: "Test DB leakage if tests share state"
      mitigation: "Each test uses a transaction that is rolled back after the test (async session fixture with rollback). Tests never commit to the shared DB."
---

<objective>
Deliver integration tests covering all Phase 3 requirements (PERS-01 through PERS-08).
Verify dedup correctness, pagination, access isolation, migration safety, and usage
event tracking.

Depends on 03.B (analysis_service) and 03.C (history endpoints). Wave 3 — runs after
all functional code is in place.
</objective>

<tasks>

<task id="03.E.1" type="execute">
<title>Add DB test fixtures to conftest.py</title>
<read_first>
- backend/tests/conftest.py
- backend/src/db/engine.py
- backend/src/db/models.py
</read_first>
<files>
- backend/tests/conftest.py
</files>
<action>
Add to `backend/tests/conftest.py`:

1. **`test_db_url` fixture:** Returns `os.environ.get("TEST_DATABASE_URL", os.environ.get("DATABASE_URL"))`.

2. **`async_engine` fixture (session scope):**
   ```python
   @pytest.fixture(scope="session")
   async def async_engine(test_db_url):
       from sqlalchemy.ext.asyncio import create_async_engine
       engine = create_async_engine(test_db_url, pool_pre_ping=True)
       yield engine
       await engine.dispose()
   ```

3. **`setup_db` fixture (session scope):**
   Runs `alembic upgrade head` to set up schema (or uses `Base.metadata.create_all`).

4. **`db_session` fixture (function scope):**
   ```python
   @pytest.fixture
   async def db_session(async_engine, setup_db):
       from sqlalchemy.ext.asyncio import AsyncSession
       async with AsyncSession(async_engine) as session:
           async with session.begin():
               yield session
               await session.rollback()
   ```

5. **`test_user` fixture:** Inserts a test user row and returns the user_id.

6. **`test_api_key` fixture:** Inserts a test api_key row for the test user.

7. **`authed_user` fixture:** Returns an `AuthedUser` instance using the test user/key IDs.

8. **`client` fixture:** Returns an `httpx.AsyncClient` configured with the FastAPI test app
   and auth headers, with the `get_db_session` dependency overridden to use the test session.

Import pytest-asyncio: `import pytest_asyncio` (or use `@pytest.fixture` with async).
</action>
<acceptance_criteria>
- `grep 'async_engine' backend/tests/conftest.py` matches
- `grep 'db_session' backend/tests/conftest.py` matches
- `grep 'test_user' backend/tests/conftest.py` matches
- `grep 'authed_user' backend/tests/conftest.py` matches
- `grep 'rollback' backend/tests/conftest.py` matches
</acceptance_criteria>
</task>

<task id="03.E.2" type="execute">
<title>Create test_analysis_service.py</title>
<read_first>
- backend/src/services/analysis_service.py
- backend/tests/conftest.py
</read_first>
<files>
- backend/tests/test_analysis_service.py
</files>
<action>
Create `backend/tests/test_analysis_service.py` with these test cases:

1. **test_compute_mesh_hash_deterministic:** Same bytes -> same hash. Different bytes -> different hash.
2. **test_compute_mesh_hash_sha256_format:** Output is 64-char hex string.
3. **test_compute_process_set_hash_sort_invariant:** `["fdm","sla"]` and `["sla","fdm"]` produce the same hash.
4. **test_compute_process_set_hash_different_sets:** `["fdm"]` and `["fdm","sla"]` produce different hashes.
5. **test_run_analysis_fresh_persists:** Mock the pipeline, call run_analysis, verify an Analysis row is created with correct mesh_hash, process_set_hash, analysis_version.
6. **test_run_analysis_cache_hit:** Insert an Analysis row, call run_analysis with same params, verify pipeline is NOT called and the returned dict matches result_json.
7. **test_run_analysis_writes_usage_event:** After run_analysis, verify a UsageEvent row exists with event_type "analysis_complete" (fresh) or "analysis_cached" (hit).
8. **test_run_analysis_version_mismatch_bypasses_cache:** Insert row with version "0.2.0", call with version "0.3.0", verify cache miss (pipeline runs).

Use `unittest.mock.patch` or `monkeypatch` to mock the pipeline execution for unit tests.
Mark async tests with `@pytest.mark.asyncio`.
</action>
<acceptance_criteria>
- `grep 'def test_compute_mesh_hash_deterministic' backend/tests/test_analysis_service.py` matches
- `grep 'def test_compute_process_set_hash_sort_invariant' backend/tests/test_analysis_service.py` matches
- `grep 'def test_run_analysis_fresh_persists' backend/tests/test_analysis_service.py` matches
- `grep 'def test_run_analysis_cache_hit' backend/tests/test_analysis_service.py` matches
- `grep 'def test_run_analysis_writes_usage_event' backend/tests/test_analysis_service.py` matches
- `grep 'analysis_cached' backend/tests/test_analysis_service.py` matches
- `grep 'analysis_complete' backend/tests/test_analysis_service.py` matches
- `grep 'asyncio' backend/tests/test_analysis_service.py` matches
</acceptance_criteria>
</task>

<task id="03.E.3" type="execute">
<title>Create test_dedup.py</title>
<read_first>
- backend/src/services/analysis_service.py
- backend/src/db/models.py
- backend/tests/conftest.py
</read_first>
<files>
- backend/tests/test_dedup.py
</files>
<action>
Create `backend/tests/test_dedup.py` with integration tests:

1. **test_same_file_same_processes_cache_hit:** Upload file A with processes ["fdm","sla"]. Upload file A again with same processes. Assert pipeline ran exactly once. Assert both responses are identical.

2. **test_same_file_different_processes_cache_miss:** Upload file A with ["fdm"]. Upload file A with ["fdm","sla"]. Assert pipeline ran twice (different process_set_hash).

3. **test_different_file_same_processes_cache_miss:** Upload file A with ["fdm"]. Upload file B with ["fdm"]. Assert pipeline ran twice (different mesh_hash).

4. **test_per_user_dedup_isolation:** User 1 uploads file A. User 2 uploads file A with same processes. Assert pipeline ran twice. Assert each user has their own Analysis row (D-13).

5. **test_cache_hit_under_200ms:** Upload file A (first time — uncached). Upload file A again. Assert second response took < 200ms (Success Criterion #2). Use `time.monotonic()` around the call.

6. **test_concurrent_duplicate_upload:** Use `asyncio.gather` to submit the same file twice concurrently. Assert exactly one Analysis row exists (IntegrityError handled). Assert both requests return the same result.

Mock the analysis pipeline where needed, but use real DB for dedup tests.
</action>
<acceptance_criteria>
- `grep 'def test_same_file_same_processes_cache_hit' backend/tests/test_dedup.py` matches
- `grep 'def test_same_file_different_processes_cache_miss' backend/tests/test_dedup.py` matches
- `grep 'def test_different_file_same_processes_cache_miss' backend/tests/test_dedup.py` matches
- `grep 'def test_per_user_dedup_isolation' backend/tests/test_dedup.py` matches
- `grep 'def test_cache_hit_under_200ms' backend/tests/test_dedup.py` matches
- `grep 'def test_concurrent_duplicate_upload' backend/tests/test_dedup.py` matches
</acceptance_criteria>
</task>

<task id="03.E.4" type="execute">
<title>Create test_history_api.py</title>
<read_first>
- backend/src/api/history.py
- backend/tests/conftest.py
</read_first>
<files>
- backend/tests/test_history_api.py
</files>
<action>
Create `backend/tests/test_history_api.py` with integration tests using httpx.AsyncClient:

1. **test_list_analyses_empty:** GET /api/v1/analyses returns `{"analyses": [], "next_cursor": null, "has_more": false}`.

2. **test_list_analyses_pagination:** Insert 25 analyses. GET with limit=10 returns 10 items, has_more=true, non-null next_cursor. GET with cursor returns next 10.

3. **test_list_analyses_cursor_traversal:** Insert 5 analyses. Walk all pages (limit=2) until has_more=false. Assert all 5 ULIDs collected, no duplicates, reverse-chronological order.

4. **test_list_analyses_verdict_filter:** Insert analyses with mixed verdicts. GET with ?verdict=pass returns only "pass" analyses.

5. **test_list_analyses_limit_clamp:** GET with ?limit=999 returns at most 100 items (or fewer if not enough data). GET with ?limit=0 or ?limit=-1 returns 400 or clamps to 1.

6. **test_get_analysis_detail:** Insert analysis, GET /api/v1/analyses/{ulid} returns the full result_json in a metadata envelope with id, filename, file_type, created_at, result fields.

7. **test_get_analysis_wrong_user:** Insert analysis for user A. Authenticate as user B. GET returns 404.

8. **test_get_analysis_nonexistent:** GET /api/v1/analyses/01NONEXISTENT returns 404.

9. **test_list_analyses_requires_auth:** GET without Authorization header returns 401.
</action>
<acceptance_criteria>
- `grep 'def test_list_analyses_empty' backend/tests/test_history_api.py` matches
- `grep 'def test_list_analyses_pagination' backend/tests/test_history_api.py` matches
- `grep 'def test_list_analyses_cursor_traversal' backend/tests/test_history_api.py` matches
- `grep 'def test_list_analyses_verdict_filter' backend/tests/test_history_api.py` matches
- `grep 'def test_get_analysis_detail' backend/tests/test_history_api.py` matches
- `grep 'def test_get_analysis_wrong_user' backend/tests/test_history_api.py` matches
- `grep 'def test_get_analysis_nonexistent' backend/tests/test_history_api.py` matches
- `grep '401' backend/tests/test_history_api.py` matches
- `grep '404' backend/tests/test_history_api.py` matches
</acceptance_criteria>
</task>

<task id="03.E.5" type="execute">
<title>Create test_migration_0002.py</title>
<read_first>
- backend/alembic/versions/0002_create_analyses_jobs_usage_events.py
- backend/alembic/versions/0001_create_users_api_keys.py
- backend/tests/conftest.py
</read_first>
<files>
- backend/tests/test_migration_0002.py
</files>
<action>
Create `backend/tests/test_migration_0002.py`:

1. **test_upgrade_creates_tables:** Run `alembic upgrade head`. Query `information_schema.tables` to assert analyses, jobs, usage_events tables exist.

2. **test_downgrade_removes_phase3_tables:** Run `alembic downgrade -1`. Assert analyses, jobs, usage_events tables do NOT exist. Assert users, api_keys still exist.

3. **test_re_upgrade_idempotent:** After downgrade, run `alembic upgrade head` again. Assert all 5 tables exist.

4. **test_dedup_index_exists:** After upgrade, query `pg_indexes` to confirm `uq_analyses_dedup` exists on the analyses table.

5. **test_analyses_columns_correct:** After upgrade, query `information_schema.columns` for the analyses table. Assert all expected columns exist with correct types (at minimum: ulid TEXT, result_json jsonb, mesh_hash TEXT).

Use subprocess to run alembic commands, or use alembic's API programmatically.
</action>
<acceptance_criteria>
- `grep 'def test_upgrade_creates_tables' backend/tests/test_migration_0002.py` matches
- `grep 'def test_downgrade_removes_phase3_tables' backend/tests/test_migration_0002.py` matches
- `grep 'def test_re_upgrade_idempotent' backend/tests/test_migration_0002.py` matches
- `grep 'def test_dedup_index_exists' backend/tests/test_migration_0002.py` matches
- `grep 'uq_analyses_dedup' backend/tests/test_migration_0002.py` matches
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `pytest backend/tests/test_analysis_service.py -v` — all tests pass
2. `pytest backend/tests/test_dedup.py -v` — all tests pass
3. `pytest backend/tests/test_history_api.py -v` — all tests pass
4. `pytest backend/tests/test_migration_0002.py -v` — all tests pass
5. All existing tests in `backend/tests/` still pass (no regressions)
</verification>

<success_criteria>
- Hash function tests verify determinism and sort-invariance
- Dedup tests verify all 4 cache key dimensions (user, mesh, processes, version)
- Per-user isolation tested explicitly (D-13)
- Pagination tests verify cursor traversal without gaps or duplicates
- Access control tested (404 for wrong user)
- Migration smoke tests verify upgrade/downgrade/re-upgrade cycle
- No regressions in existing test suite
</success_criteria>
