---
phase: 06-packaging-deploy-observability-docs
plan: 06.C
subsystem: ci-cd
tags: [ci, docker, deploy, alembic, typecheck, vercel]
dependency_graph:
  requires: [06.A]
  provides: [ci-pipeline, docker-build, image-deploy]
  affects: [.github/workflows/ci.yml, frontend/.env.example]
tech_stack:
  added: [docker-buildx, pyright, alembic-ci-check]
  patterns: [pre-built-image-deploy, gha-cache]
key_files:
  modified:
    - .github/workflows/ci.yml
    - frontend/.env.example
decisions:
  - "Pyright typecheck runs with continue-on-error:true initially (existing code may have type issues)"
  - "Frontend deploy removed from CI (Vercel GitHub integration handles it)"
  - "Docker image cached via GitHub Actions cache (type=gha)"
metrics:
  duration: 57s
  completed: "2026-04-16T17:11:00Z"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 06 Plan C: CI/CD Pipeline Expansion Summary

Expanded CI pipeline with Alembic migration check, pyright typecheck, Docker buildx build+push to Fly.io registry, and image-based deploy replacing slow --dockerfile builds.

## Tasks Completed

| Task | Title | Commit | Key Changes |
|------|-------|--------|-------------|
| 06C-1 | Expand ci.yml with migration check, Docker build, typecheck | 10b86e4 | Added alembic upgrade/downgrade check, pyright step, docker-build job with buildx+registry push, image-based deploy |
| 06C-2 | Update frontend/.env.example for Vercel | 4528e0a | Added SENTRY_DSN, SENTRY_AUTH_TOKEN, NEXT_PUBLIC_SENTRY_DSN, SENTRY_RELEASE vars with Vercel dashboard guidance |

## Deviations from Plan

None - plan executed exactly as written.

## Verification Results

- YAML validates: PASSED
- alembic upgrade head present: PASSED
- alembic downgrade -1 present: PASSED
- pyright typecheck step: PASSED
- docker-build job: PASSED
- docker/build-push-action@v5: PASSED
- registry.fly.io/cadverify-api: PASSED
- --image deploy (no --dockerfile): PASSED
- SENTRY_DSN in .env.example: PASSED
- NEXT_PUBLIC_API_BASE in .env.example: PASSED

## Known Stubs

None.

## Self-Check: PASSED
