---
phase: 07-async-sam-3d
plan: A
subsystem: jobs
tags: [arq, redis, sam3d, async, worker, alembic]

# Dependency graph
requires:
  - phase: 03-persistence
    provides: Job ORM model, async engine, session factory
  - phase: 06-packaging
    provides: docker-compose worker service, fly.toml worker process
provides:
  - JobQueue ABC protocol with enqueue/get_status/cancel
  - ArqJobQueue implementation with Redis backing
  - arq worker entrypoint with SAM-3D model preload
  - run_sam3d_job task with heuristic fallback
  - Alembic migration 0003 for idempotency index
affects: [07.B-PLAN, 07.C-PLAN]

# Tech tracking
tech-stack:
  added: [arq>=0.27.0]
  patterns: [ABC protocol for job queue, module-level Redis pool singleton, DB-driven job status]

key-files:
  created:
    - backend/src/jobs/__init__.py
    - backend/src/jobs/protocols.py
    - backend/src/jobs/arq_backend.py
    - backend/src/jobs/worker.py
    - backend/src/jobs/tasks.py
    - backend/alembic/versions/0003_add_jobs_idempotency_index.py
  modified:
    - backend/requirements.txt
    - backend/src/segmentation/sam3d/config.py

key-decisions:
  - "Used get_session_factory()/get_engine() instead of plan's init_engine/async_session_factory -- matched existing engine.py API"
  - "DB-driven status tracking (not arq introspection) for reliable job status queries"

patterns-established:
  - "JobQueue ABC: all queue implementations go through protocols.py interface"
  - "Worker startup hook: model preload pattern for GPU-bound tasks"

requirements-completed: [SAM-01]

# Metrics
duration: 3min
completed: 2026-04-16
---

# Phase 7 Plan A: JobQueue Protocol + arq Backend + Worker Entrypoint Summary

**arq job queue with JobQueue ABC protocol, SAM-3D worker entrypoint with model preload, and idempotency migration**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-16T17:36:46Z
- **Completed:** 2026-04-16T17:39:28Z
- **Tasks:** 4
- **Files modified:** 8

## Accomplishments
- JobQueue ABC protocol with enqueue/get_status/cancel and ArqJobQueue implementation
- arq worker entrypoint with SAM-3D backbone preload at startup, 10-min timeout, max 2 retries
- run_sam3d_job task with automatic heuristic fallback on SAM-3D failure
- Alembic migration 0003 adding UNIQUE (analysis_id, job_type) idempotency index
- SAM3D config defaults updated for production paths (Fly volume + pre-baked model)

## Task Commits

Each task was committed atomically:

1. **Task 1: JobQueue protocol and arq backend** - `bac1b26` (feat)
2. **Task 2: Worker entrypoint and SAM-3D task** - `3de1cf6` (feat)
3. **Task 3: Alembic idempotency migration** - `b195b73` (feat)
4. **Task 4: SAM3D config production defaults** - `a018b0e` (feat)

## Files Created/Modified
- `backend/src/jobs/__init__.py` - Package init
- `backend/src/jobs/protocols.py` - JobQueue ABC, JobStatus enum, JobInfo dataclass
- `backend/src/jobs/arq_backend.py` - ArqJobQueue with Redis pool, FastAPI dependency
- `backend/src/jobs/worker.py` - WorkerSettings with SAM-3D startup hook
- `backend/src/jobs/tasks.py` - run_sam3d_job with heuristic fallback
- `backend/alembic/versions/0003_add_jobs_idempotency_index.py` - UNIQUE index migration
- `backend/requirements.txt` - Added arq>=0.27.0
- `backend/src/segmentation/sam3d/config.py` - Production defaults for model_path and cache_dir

## Decisions Made
- Used `get_session_factory()`/`get_engine()` instead of plan's `init_engine`/`async_session_factory` -- the existing engine.py uses lazy singleton pattern, not explicit init
- DB-driven status tracking for get_status (queries Job table directly) rather than arq job introspection for reliability

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adapted worker.py to existing engine.py API**
- **Found during:** Task 2 (worker entrypoint)
- **Issue:** Plan referenced `init_engine()` and `async_session_factory()` which don't exist in engine.py; actual API is `get_engine()` and `get_session_factory()`
- **Fix:** Used `get_engine()` in startup and `get_session_factory()` in tasks.py
- **Files modified:** backend/src/jobs/worker.py, backend/src/jobs/tasks.py
- **Verification:** All 4 module imports succeed
- **Committed in:** 3de1cf6 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary adaptation to match existing codebase API. No scope creep.

## Issues Encountered
- Pre-existing test_health failure (503 in test env due to missing DB/Redis) -- not caused by this plan, not fixed.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Job queue infrastructure ready for 07.B (API routes: POST /jobs, GET /jobs/:id, DELETE /jobs/:id)
- Worker entrypoint ready for 07.C (Docker/Fly worker process configuration)

---
*Phase: 07-async-sam-3d*
*Completed: 2026-04-16*
