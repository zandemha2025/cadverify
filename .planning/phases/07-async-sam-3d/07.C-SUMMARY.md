---
phase: 7
plan: C
subsystem: sam3d-infra
tags: [docker, fly, worker, db-lifecycle, integration-tests]
dependency_graph:
  requires: [07.A, 07.B]
  provides: [sam3d-deployment-ready, worker-always-on, fallback-tested]
  affects: [backend/Dockerfile, backend/fly.toml, backend/src/db/engine.py, backend/.env.example]
tech_stack:
  added: []
  patterns: [pre-baked-weights, init-engine-lifecycle, worker-always-on]
key_files:
  created:
    - backend/tests/test_sam3d_worker.py
  modified:
    - backend/Dockerfile
    - backend/fly.toml
    - backend/src/db/engine.py
    - backend/src/jobs/worker.py
    - backend/.env.example
decisions:
  - "Used ADD instruction for SAM-2 weights download (Docker layer caching)"
  - "Added init_engine() as thin wrapper over existing get_engine + get_session_factory (minimal change)"
  - "Worker gets shared /data mount for blob and cache access"
metrics:
  duration: 150s
  completed: "2026-04-15"
  tasks: 5
  files: 6
---

# Phase 7 Plan C: Pre-baked Weights + Embedding Cache + Visibility + Fallback Summary

SAM-2 Hiera Small weights pre-baked into Docker image at /app/models, Fly worker configured always-on with shared /data volume, DB engine init/dispose lifecycle hooks for arq worker, and 6 integration tests covering SAM-3D success/fallback/serialization paths.

## Task Completion

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Add SAM-2 weights to Dockerfile | a5302dc | backend/Dockerfile |
| 2 | Update Fly.toml for worker config | 7ec039a | backend/fly.toml |
| 3 | Add worker DB engine lifecycle hooks | 3b44c29 | backend/src/db/engine.py, backend/src/jobs/worker.py |
| 4 | Update .env.example with SAM-3D vars | 7f2dd17 | backend/.env.example |
| 5 | Write worker integration tests | 70ee02c | backend/tests/test_sam3d_worker.py |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed mock patch target for segment_sam3d**
- **Found during:** Task 5
- **Issue:** `segment_sam3d` is imported locally inside `run_sam3d_job`, so `patch("src.jobs.tasks.segment_sam3d")` fails with AttributeError
- **Fix:** Changed patch target to `src.segmentation.sam3d.pipeline.segment_sam3d`
- **Files modified:** backend/tests/test_sam3d_worker.py
- **Commit:** 70ee02c

## Verification Results

- Dockerfile contains sam2.1_hiera_small.pt: PASS
- fly.toml contains auto_stop for worker: PASS
- .env.example contains SAM3D_ENABLED: PASS
- init_engine/dispose_engine importable: PASS
- All 6 worker tests pass: PASS
