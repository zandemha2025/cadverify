---
phase: 11
plan: "04"
title: "Report integration + tolerance achievability output + PDF"
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - backend/src/analysis/models.py
  - backend/src/services/pdf_service.py
  - backend/src/api/routes.py
  - backend/tests/test_tolerance_report_integration.py
autonomous: true
requirements:
  - STEP-05
---

<objective>
Complete the user-facing integration: extend AnalysisResult with an optional tolerances field, update the /validate endpoint response to include tolerance data when available, update the PDF template with a tolerance achievability table, and create integration tests that verify the full STEP AP242 -> GD&T -> validation -> report pipeline end-to-end.
</objective>

<must_haves>
- AnalysisResult extended with optional tolerances field (backward-compatible)
- /validate endpoint response includes tolerances section for AP242 STEP files
- PDF template renders tolerance table when present
- End-to-end integration test from STEP upload to tolerance report
- Existing STL responses unchanged (no tolerances key)
</must_haves>

<tasks>

<task id="11-04-01" type="execute">
<title>Extend AnalysisResult model with optional tolerances</title>
<files>backend/src/analysis/models.py</files>
<read_first>
- backend/src/analysis/models.py (MUST read current full file — adding field to existing AnalysisResult)
- backend/src/analysis/tolerance_models.py (ToleranceReport type being referenced)
</read_first>
<action>
Modify `backend/src/analysis/models.py`:

1. Add import at top:
   ```python
   from src.analysis.tolerance_models import ToleranceReport
   ```

2. Add optional field to `AnalysisResult` dataclass (AFTER existing fields, before methods):
   ```python
   tolerances: Optional[ToleranceReport] = None
   ```

This is backward-compatible: existing code that creates AnalysisResult without tolerances will get `None` (default). The field is only populated when STEP AP242 with PMI is analyzed.
</action>
<acceptance_criteria>
- `backend/src/analysis/models.py` contains `from src.analysis.tolerance_models import ToleranceReport`
- `backend/src/analysis/models.py` `AnalysisResult` class contains `tolerances: Optional[ToleranceReport] = None`
- Existing AnalysisResult instantiation (without tolerances argument) still works
</acceptance_criteria>
</task>

<task id="11-04-02" type="execute">
<title>Update _to_response in routes.py to include tolerances</title>
<files>backend/src/api/routes.py</files>
<read_first>
- backend/src/api/routes.py (MUST read current full file — modifying _to_response function)
- backend/src/services/tolerance_service.py (tolerance_report_to_dict function)
</read_first>
<action>
Modify `backend/src/api/routes.py`:

1. The tolerance data is already added to `result_dict` by `analysis_service.py` (task 11-03-04) after calling `_to_response()`. Verify this integration works correctly by checking that the response dict flows through without dropping the `tolerances` key.

2. If `_to_response()` constructs the response dict from scratch (rather than using `result_dict` directly), add tolerance inclusion:
   ```python
   # Inside _to_response or the response construction:
   if hasattr(result, 'tolerances') and result.tolerances is not None:
       from src.services.tolerance_service import tolerance_report_to_dict
       response["tolerances"] = tolerance_report_to_dict(result.tolerances)
   ```

3. Ensure the OpenAPI schema reflects the optional tolerances field by updating any Pydantic response models if used. If the endpoint returns raw dicts (which it does based on the existing code pattern), the tolerances key will appear automatically for STEP AP242 files.

The key point: tolerances should appear in the response ONLY for STEP files with PMI. STL responses must not include a tolerances key at all.
</action>
<acceptance_criteria>
- The `/validate` endpoint response includes a `tolerances` key when a STEP AP242 file with PMI is uploaded
- The `/validate` endpoint response does NOT include a `tolerances` key when an STL file is uploaded
- No changes break the existing response structure for STL or plain STEP files
</acceptance_criteria>
</task>

<task id="11-04-03" type="execute">
<title>Update PDF template with tolerance table</title>
<files>backend/src/services/pdf_service.py</files>
<read_first>
- backend/src/services/pdf_service.py (MUST read current full file — adding tolerance section to PDF template)
- backend/src/analysis/tolerance_models.py (ToleranceReport, ToleranceEntry, AchievabilityVerdict)
</read_first>
<action>
Modify `backend/src/services/pdf_service.py`:

1. In the Jinja2 HTML template (the string template used for WeasyPrint rendering), add a conditional tolerance section AFTER the process scores section:

```html
{% if tolerances and tolerances.has_pmi %}
<div class="section">
  <h2>Tolerance Achievability</h2>
  {% if tolerances.pmi_note %}
  <p class="note">{{ tolerances.pmi_note }}</p>
  {% endif %}
  <p><strong>Summary Score:</strong> {{ tolerances.summary_score|round(1) }}% of specified tolerances achievable by recommended process</p>
  <table class="tolerance-table">
    <thead>
      <tr>
        <th>ID</th>
        <th>Type</th>
        <th>Value (mm)</th>
        <th>Datums</th>
        <th>Best Process</th>
        <th>Verdict</th>
      </tr>
    </thead>
    <tbody>
    {% for entry in tolerances.entries %}
      <tr>
        <td>{{ entry.id }}</td>
        <td>{{ entry.type }}</td>
        <td>{{ entry.value_mm }}</td>
        <td>{{ entry.datum_refs|join(', ') }}</td>
        <td>{{ entry.best_process }}</td>
        <td class="verdict-{{ entry.best_verdict }}">{{ entry.best_verdict }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endif %}
```

2. Add CSS styles for the tolerance table:
```css
.tolerance-table { width: 100%; border-collapse: collapse; margin: 10px 0; }
.tolerance-table th, .tolerance-table td { border: 1px solid #ddd; padding: 6px 8px; text-align: left; font-size: 10px; }
.tolerance-table th { background-color: #f5f5f5; font-weight: bold; }
.verdict-achievable { color: #2e7d32; }
.verdict-marginal { color: #f57f17; }
.verdict-not_achievable { color: #c62828; }
```

3. In the template data preparation function, add tolerance data transformation:
   - If `result_dict` has a `tolerances` key with `has_pmi: true`, transform entries for the template.
   - For each tolerance entry, find the best-achieving process and its verdict.
   - Pass transformed data to Jinja2 context.

The tolerance table section should only render when `tolerances.has_pmi` is true. For STL files and STEP files without PMI, this section is completely absent from the PDF.
</action>
<acceptance_criteria>
- `backend/src/services/pdf_service.py` contains `tolerance-table` CSS class
- `backend/src/services/pdf_service.py` contains `{% if tolerances and tolerances.has_pmi %}`
- Template renders tolerance table headers: ID, Type, Value (mm), Datums, Best Process, Verdict
- Verdict cells have color-coded CSS classes (achievable=green, marginal=amber, not_achievable=red)
- PDF generation for STL files is unaffected (no tolerance section rendered)
</acceptance_criteria>
</task>

<task id="11-04-04" type="execute">
<title>Create end-to-end integration tests</title>
<files>backend/tests/test_tolerance_report_integration.py</files>
<read_first>
- backend/src/services/analysis_service.py (run_analysis function — the integration point)
- backend/src/services/tolerance_service.py (tolerance analysis called from here)
- backend/tests/test_step_parser.py (test patterns for STEP file testing)
</read_first>
<action>
Create `backend/tests/test_tolerance_report_integration.py` with:

1. `test_stl_analysis_no_tolerances()`:
   - Mock an STL file analysis through `run_analysis`.
   - Verify response dict does NOT contain a `tolerances` key.
   - This is the regression test ensuring STL path is unaffected.

2. `test_step_analysis_with_tolerance_fallback()`:
   - Mock a STEP file analysis where `is_ap242_supported()` returns False.
   - Verify the response either has no `tolerances` key or has `tolerances.has_pmi = False`.

3. `test_step_analysis_tolerance_error_graceful()`:
   - Mock a STEP file analysis where `analyze_tolerances` raises an exception.
   - Verify the response has `tolerances.has_pmi = False` with error note (not an HTTP 500).

4. `test_tolerance_report_dict_structure()`:
   - Create a full ToleranceReport with 3 tolerance entries and achievability data.
   - Call `tolerance_report_to_dict`.
   - Verify dict structure: `has_pmi`, `summary_score`, `pmi_note`, `entries` list.
   - Verify each entry in `entries` has: `id`, `type`, `value_mm`, `datum_refs`, `achievability` dict.
   - Verify achievability dict is keyed by process type string with `verdict`, `capability`, `margin`.

5. `test_pdf_template_with_tolerances()`:
   - If WeasyPrint available: render a PDF with mock tolerance data, verify it generates without error.
   - If WeasyPrint not available: skip with appropriate mark.

Use `unittest.mock.patch` and `pytest.mark.skipif` as needed.
</action>
<acceptance_criteria>
- `backend/tests/test_tolerance_report_integration.py` exists
- File contains `def test_stl_analysis_no_tolerances` (regression guard)
- File contains `def test_step_analysis_tolerance_error_graceful`
- File contains `def test_tolerance_report_dict_structure`
- `pytest backend/tests/test_tolerance_report_integration.py -v` passes
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `pytest backend/tests/test_tolerance_report_integration.py -v` passes
- [ ] `pytest backend/tests/ -v` full test suite passes (no regression)
- [ ] STL upload to `/validate` returns response WITHOUT tolerances key
- [ ] STEP upload (when OCP available + PMI present) returns response WITH tolerances section
- [ ] PDF generation works for both STL (no tolerance table) and STEP+PMI (with tolerance table)
</verification>
