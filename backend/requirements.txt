fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
trimesh[easy]>=4.4.0
# STEP/STP ingestion: gmsh embeds the OpenCASCADE kernel to read STEP and
# tessellate to a trimesh shell (DFM + cost). py2.py3 wheels exist for macOS
# arm64 + manylinux x86_64; if no wheel for the deploy target, the route
# degrades to a clean 501 via is_step_supported() (no crash). ~36 MB wheel.
gmsh>=4.13
numpy>=1.26.0
scipy>=1.13.0
shapely>=2.0.0
pydantic>=2.7.0
pymeshfix>=0.17
pyyaml>=6.0
rtree>=1.0
argon2-cffi==23.1.0
authlib==1.7.2
resend==2.33.0
sqlalchemy==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1
python-ulid>=3.0.0,<4.0.0
alembic==1.18.5
redis==5.2.0
httpx==0.28.1
respx==0.23.1
pytest-asyncio==0.24.0
itsdangerous>=2.2.0
slowapi>=0.1.9
structlog==24.4.0
sentry-sdk==2.65.0
# Prometheus text-exposition for the /metrics scrape target (enterprise
# observability). Pure-python, no system deps.
prometheus-client>=0.20.0
fakeredis==2.36.2
weasyprint>=62.0
jinja2>=3.1.0
scalar-fastapi>=1.8.0
arq>=0.27.0
# Local TripoSR GPU inference uses optional heavy dependencies (torch + tsr)
# installed on GPU workers, not in the base API image.
rembg>=2.0.0
Pillow>=10.0.0
python3-saml>=1.16.0
