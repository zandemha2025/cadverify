{{- if .Values.saml.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cadverify.fullname" . }}-saml
  labels:
    {{- include "cadverify.labels" . | nindent 4 }}
type: Opaque
stringData:
  sp-key.pem: ""
  sp-cert.pem: ""
{{- end }}
