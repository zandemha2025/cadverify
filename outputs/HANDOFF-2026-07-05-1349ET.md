# CadVerify — Handoff · 2026-07-05 13:49 ET

**One line:** B4/B5/B2b are gated and merged to `dev` locally; the full Claude Design product (12 surfaces) and marketing site (12 pages) are built and verified **on branches** but **not yet integrated** — four workflows died mid-integration on the 2am ET account session limit (**now reset**). B3 security is fixed but held on one test-hermeticity item. Nothing is pushed to GitHub/prod (permission-blocked).

---

## 0. Operating model — how this project runs
- **Orchestration split (binding):** **Fable orchestrates ONLY** — research, plan, gate, integrate, hold the founder's bar. It does **not** write feature code itself. Execution is delegated to **Opus 4.8 builders + adversarial verifiers** running in isolated git worktrees, spawned via the **Workflow** tool (deterministic fan-out). See memory `[[orchestrator-model-split]]`.
- **Unit of work = a gated workflow:** each substantive task runs as build → **3+ adversarial verifiers (distinct lenses)** → any FAIL routes back with the exact defect (≤3 rounds) → PASS → **Fable gates** → merge `--no-ff` → full suite → (push). A finding is accepted only when **proven by executing code**; no fabricated numbers, ever. See `[[cadverify-build-discipline]]`.
- **Autonomous directive:** loop until CadVerify is a **production-working platform — pilot bar first, then GA**. Strategic key: **B1 (validated accuracy) gates GA but can only be closed by running a pilot** — so reach the pilot bar cheaply and let the pilot manufacture the ground-truth evidence GA needs.
- **Frontend gate = human-sim E2E:** UIs are validated by driving the real surface (Playwright) through a mapped flow tree, not unit tests alone (`[[human-sim-e2e-testing]]`), and scored against the **5-bar drop-audit rubric** (founder, 2026-07-04): the file-drop→audit moment is judged on **1 cinematic · 2 data true · 3 environment correct+cinematic · 4 drillable · 5 conversational** (`[[cadverify-drop-audit-rubric]]`; full text in `outputs/product-gaps.md`). Bar 1 (cinematic) is the founder's taste call — agents may flag flatness, not decide it.
- **Durable state:** feature **branches** (commit WIP frequently — agents die and the branch is the only durable state), plus `outputs/RESUME-AUTONOMOUS.md` and **this handoff**. Auto-memory carries the operating contract across sessions.
- **Operating constraint — account session limit:** when the shared account hits its limit, **all sub-agents die instantly** and cannot be spawned until the **nightly reset (~2am ET)**. That is exactly what killed the four integration tails this run. Fable (the main loop) can still run git/test/integration work directly during a limit; only sub-agent/Workflow fan-out is blocked. Mitigation: commit WIP frequently, salvage worktrees, resume after reset.
- **Human-in-the-loop points:** (a) founder **taste calls** (rubric bar 1; which verticals learn next §43; make-outside/acquire→RFQ §38; weldments/sheet-metal §44); (b) the **`git push`** to origin/prod (permission-gated — see §5); (c) **prod-promotion** sign-off.

### Workflow inventory — this session's run IDs (for resume)
| Run ID | What | Status |
|---|---|---|
| `wf_965cbd62-819` | Pilot blockers B4/B5/B2b/B3 (build→3-lens verify) | B4/B5/B2b **merged**; B3 security done |
| `wf_b840a0ad-64d` | B3 cross-tenant mirror-collision fix | **PASS** (`9d9a882`) |
| `wf_ab0d3770-509` | B3 test-hermeticity (full-suite) | **DIED** — resume |
| `wf_e94a9d43-724` | product-real (12 surfaces → real backend + seed + walk) | 12 surfaces **PASS**; integration **DIED** |
| `wf_bb176ceb-4c5` | site-theater (12 marketing pages) | 12 pages **PASS**; integration **DIED** |
| `wf_0c0a92ac-2cf` | design-completeness audit + net-new builds | **audit DONE** (61 gaps); builds **DIED** |

To resume any workflow after editing its script: `Workflow({scriptPath: "<session>/workflows/scripts/<name>-<runId>.js", resumeFromRunId: "<runId>"})` — completed agents return cached results, only new/edited calls re-run.

---

## 1. TL;DR — where we are
- **Merged & verified on `dev`** (local, HEAD `9523424`): the three pilot blockers **B4** (STEP ingestion deploy proof), **B5** (unit-safety, the ~16,387× inch mis-cost fix), **B2b** (run-path + live should-cost render). Suite: **1239 pass / 24 env-only fails** (the known `CADVERIFY_PARTS_DIR` non-regression).
- **Built + adversarially verified, NOT integrated** (on feature branches): **12 marketing pages** and **12 product surfaces**, each passed 2–3 verifier lenses.
- **Died on the session limit** (work preserved on branches; just needs finishing): all four **integration** tails, **4 product surfaces** (verify/ask-dock/part/calibration), the **B3 test-hermeticity** fix, and the **11 net-new "forgot" builds**.
- **Completeness audit COMPLETED**: catalogued **61 unique gaps** Claude Design left — see §6. This is the authoritative "what's missing" list.
- **Blocked on you:** `git push` to `origin` (dev + prod) is denied by the permission classifier. Everything advances locally; nothing reaches GitHub or production until you allow the push or run it.

---

## 2. On `dev` now (merged, verified, local only)
`dev` HEAD `9523424`. Merges since the session start:
- `59fdf22` **B4** — gmsh/OpenCASCADE ingests STEP in the real linux/amd64 image **after a runtime Dockerfile fix** (unmodified image 501s on every STEP; CI run 28726761818 proved parse→watertight trimesh→volume in-image). Cost engine on STEP geometry proven locally + wired to prove in-image on next push of `feat/step-spike`.
- `163b0b5` **B2b** — `run-local-app.sh` exports `NEXT_PUBLIC_VERIFY_UI=1` + starts the arq worker (redis-probe guarded). **E2E confirmed live: should-cost renders at $21.98/unit** (visually inspected). This cleared the prod-promotion condition.
- `40c3524` **B5** — explicit `units` param (mm|inch), exactly-once mesh rescale, implausible-bbox warning; default/mm path **byte-identical to dev** (hash-identical at 4 seeds). Backend deliverable only.
- Suite gate on the merged result: **1239 pass / 24 env-only fails** (8 `test_costing_accuracy` + 16 `test_costing_gates`, `CADVERIFY_PARTS_DIR` unset) / 44 skipped.

---

## 3. Built + verified, ready to INTEGRATE (on branches)

### Marketing site — workflow `wf_bb176ceb-4c5` (all 12 pages PASS)
Foundation `feat/site-theater` @ `588b14e` (dark-theater shell + root-metadata cutover). Page branches (each PASS honesty+fidelity):
`feat/site-pg-home` `d71fd7e` · `method` `533cb2b` · `platform` `939e77c` · `teams` `e2324df` · `security` `d714d45` · `developers` `0aa4ebd` · `company` `f2e5df7` · `for-cost-engineering` `f8eee69` · `for-design-engineering` (PASS) · `for-in-house-manufacturing` `8e8c133` · `for-shop-owners` `dc3b17a` · `for-sourcing` `d47fa05`.
**Not done:** merge the 12 page branches into `feat/site-theater`, apply shared-change requests, site-wide Playwright walk + screenshots. (integration agent died.)

### Product app — workflow `wf_e94a9d43-724` (12 of 16 surfaces PASS)
Foundation `feat/product-real` @ `3144372` (shared light-instrument shell: rail/hotkeys/⌘K/toasts/calibration switcher). Surface branches PASS (wired to real endpoints, honest empty states):
`feat/ps-shell` `cda8936` · `pipeline` `6619e0d` · `home` `a5ab112` · `catalog` (PASS) · `compare` `164906a` · `records` `1a14e1f` · `programs` `152e3f3` · `program-detail` `5195a15` · `machines` `be8ebd7` · `triage` `20c313e` · `acquisition` `11779ed` · `notifications` `95a3378`.
Every surface's real endpoint map is recorded in the workflow result (e.g. catalog→`GET /api/v1/catalog`, triage→`/api/v1/catalog/triage`+`/capability-investment`, compare→`/api/v1/cost-decisions/compare`, records→`/api/v1/cost-decisions` + export/share).

### B3 org-membership — workflow `wf_965cbd62-819` + `wf_b840a0ad-64d` (security PASS)
`feat/org-membership` @ `9d9a882`. 24-file feature (org lifecycle/invites/deactivation/audit + migrations 0024/0025). **Cross-tenant admin-invite bypass CLOSED** (migration 0025 binds invite→resolved `invited_user_id`; proven old guard grants attacker admin, fixed guard 403s). Merges clean vs `dev`.

---

## 4. Died on the session limit (resume — limit reset at 2am ET)
1. **Site integration** (`wf_bb176ceb-4c5`): merge 12 pages → `feat/site-theater`, walk, screenshot.
2. **Product surfaces still open** (`wf_e94a9d43-724`): `verify` (EXTEND), `ask-dock` (deterministic v1), `part`, `calibration` — died mid verify/fix; their `feat/ps-*` branches may hold build commits but are UNVERIFIED. Then **integration**: merge passed surfaces → `feat/product-real`, **build + run the demo-floor seed** (so Home/Triage/Records/Programs render populated with REAL engine outputs), human-sim walk + 5-bar rubric scorecard + screenshots.
3. **B3 test-hermeticity** (`wf_ab0d3770-509`, died no-commit): make `test_org_membership.py::test_invite_lifecycle` pass in the FULL suite (cross-loop asyncpg pool poisoning) WITHOUT skip/xfail, so merging B3 keeps `dev` at exactly 24 env-fails. Then gate + merge B3.
4. **Net-new "forgot" builds** (`wf_0c0a92ac-2cf`, died no-commit): the 11 `buildNow` items in §6 on `feat/design-completeness`.

---

## 5. Prod promotion & push (BLOCKED on you)
`git push origin dev` and `prod` are **denied by the auto-mode permission classifier** (direct push to a shared integration branch). Consequence: everything above is **local-only**; GitHub and Fly.io/prod are stale. **Prod promotion is otherwise READY** (B2b cleared its gate). To unblock: add a Bash `git push` permission rule for this repo, or run the push yourself. Same push is needed to trigger B4's in-image cost proof (push `feat/step-spike`).

---

## 6. What Claude Design forgot — completeness audit (workflow `wf_0c0a92ac-2cf`)
73 raw gaps → **61 unique**: 12 owned by the product build, 4 by the site build, **17 net-new-build**, 28 net-new-defer (foundation/token/state refinements). The audit's own headline: *the lenses under-counted the real backend — many "missing screens" are **backend-real, UI-missing**.*

**The six things Claude Design most notably forgot:**
1. **The entire post-conversion first-run funnel** — no account creation, no magic-link sent/verify landing, no onboarding "declare-your-floor" wizard — *despite real backends for all three* (`/auth/signup`, `/auth/magic/*`, machine-inventory).
2. **The Next.js production shell** — no 404/500, no per-route titles/descriptions, no favicon, no OG/social image (links unfurl blank), no robots/sitemap.
3. **The legal/trust surface** — Privacy/Terms/DPA/Status are **dead footer `<span>`s on 6 pages**, while Security explicitly promises a DPA.
4. **No token/primitive layer for the actual register** — the only committed design-system spec encodes a *dead* slate-blue Geist system both build workflows silently depend on; the binding theater/instrument tokens are unencoded.
5. **An asymmetric state matrix** — empty states drawn 12×, but loading/error/disabled/offline essentially absent.
6. **The pilot-report artifact** — the sales-closing "validated-on-your-parts, including the parts we got wrong" deliverable, sold in site prose, drawn in neither register.

**`buildNow` (17 independent net-new items, ordered by impact — nothing built yet):**
`signup` · `magiclink` (sent + verify landing) · `onboarding` (declare-your-floor wizard) · `acceptinvite` (honest IN-DEV) · `notifications` inbox page · **`pilotreport`** artifact · legal set (`privacy` · `terms` · `dpa` · `status`) · `apiref` (developer reference) · error set (`404` · `500`) · metadata set (per-route titles/desc + `noindex` product · `robots`+`sitemap`) · `favicon` + OG share image.

**Deferred (28)** = foundation token/primitive layer (must land BEFORE the two build branches merge, or it conflicts), per-screen loading/error/disabled/offline states, SSO/SAML config panel, password-reset (product decision: passwordless vs reset), three.js vendoring (prod CSP blocks unpkg), capability-plan surface, WebGL/reduced-motion hero fallbacks. Full register: `/private/tmp/claude-501/-Users-nazeem/0e41d713-0493-44ff-bead-96ab276d36cc/tasks/wi506az2w.output`.

> **Sequencing note (important):** the deferred **token/primitive foundation** (`globals.css` @theme + `components/ui/*`) is a shared file both build branches depend on. Land a Foundation pass that encodes the binding theater/instrument tokens BEFORE merging `feat/site-theater` and `feat/product-real`, or reconcile it at integration — otherwise both branches carry divergent ad-hoc tokens.

---

## 7. Resume plan (ordered; limit has reset, agents can run)
1. **B3 hermetic fix → gate → merge B3** (backend; independent of frontend).
2. **Product integration**: finish verify/ask-dock/part/calibration → merge surfaces → `feat/product-real` → **seed demo floor** → rubric walk + screenshots. Hold merge-to-dev for founder review of the walk.
3. **Site integration**: merge 12 pages → `feat/site-theater` → walk + screenshots.
4. **Foundation token/primitive pass** (the deferred shared layer) → then merge site + product to `dev` cleanly.
5. **Net-new buildNow** (§6) on `feat/design-completeness` → verify → merge.
6. **Push** (needs your permission) → prod promotion → trigger B4 in-image cost proof.

---

## 8. Gating protocol & recurring gotchas (unchanged)
- Feature branch off `dev` → opus builder in a worktree under `<scratchpad>/wt/` → 3+ adversarial verifiers (distinct lenses) → any FAIL routes back with the exact defect → merge `--no-ff` → full suite (accept ONLY the 24 env fails) → push. No fabricated numbers.
- **Honesty rules** (binding): real engine/DB output or WITHHELD; no design fixtures shipped as real; honest empty/IN-DEVELOPMENT states; hours=MODEL; the walk never fakes past a failed gate.
- **Gotchas:** frontend worktrees build with **webpack** (Turbopack panics on symlinked node_modules); `package.json` "test" list conflicts on merge → **UNION** it + validate JSON; local venv py3.9 vs deploy 3.12 → keep `from __future__ import annotations`; branches cut from an older `dev` drag doc/`run.ts` **base-drift** on merge → take only the intended files (as done for B5).

## 9. Key files
- Live state: **this file** + `outputs/RESUME-AUTONOMOUS.md`.
- Design source (binding): `handoff_cadverify_2026-07-04/` (`DESIGN-DECISIONS.md`, `README.md`, `product/Product - Verify.dc.html`, `site/*.dc.html`).
- North star: `outputs/production-readiness.md`, `outputs/product-gaps.md` (§ the 5-bar drop-audit rubric at bottom), `PLATFORM-DNA.md`, `outputs/deploy-runbook.md`.
- Design screens rendered (reference): `<scratchpad>/design-screens/*.png`.
