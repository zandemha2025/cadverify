# CadVerify — Human-Sim BASELINE Scorecard (Full Rubric)

- **Date:** 2026-07-09
- **Run type:** First full-rubric human-sim baseline. Vision-judged, real browser (Playwright/chromium headless) driving the live stack at `http://localhost:3041`.
- **Stack:** worktree `/home/user/cadverify/.claude/worktrees/hsim-base` (HEAD f9dcf94). Backend uvicorn :8041 (postgres:true, `cadverify_hsim`), frontend next dev :3041. RUNBOOK env set exactly (incl. `DASHBOARD_SESSION_SECRET`). Smoke gate: `/health` postgres:true; `/auth/signup` → **HTTP 200 + session token**.
- **Persona:** P1/P5 blend (design engineer + skeptical exec), brand-new empty org.
- **Evidence dir:** `/home/user/cadverify/outputs/human-sim/framework/scorecards/baseline-shots/`
- **Driver:** `baseline-shots/driver.mjs` (+ `a11y.mjs`, `refresh.mjs` probes).

## 12-category vector

| # | Category | Score | Note |
|---|---|---|---|
| 1 | Functional correctness | **85** | W12/W1/W2/W3/W11-onboard/spots all correct; W11 revision-identity card never fires (honest, not broken) |
| 2 | Data fidelity | **90** | Provenance on every driver; line items reconcile to unit cost; FACT qty; NACE citations correct; previews withheld |
| 3 | AI fidelity | **90** | No hallucination (torus + revision honestly "none confident enough"); env exclusions technically correct; honest uncertainty |
| 4 | Interaction fidelity | **88** | Uploads, rail nav, env toggles→re-verify, material switch, part-tree select, onboard pickers all behave |
| 5 | Visual fidelity | **88** | Real tessellated renders; clean layout; transient sparse-wireframe on assembly camera-in |
| 6 | Performance (real ms) | **70** | page-load ~1.4–1.65s good; single-part verify **13.3–22.1s**, assembly **34s** — the honest limiter |
| 7 | Reliability (3×) | **92** | W1 ×3 → identical $8.68/FDM/hatched n=0; deterministic |
| 8 | Conversational | **N/A** | NL "Ask the engine" surface exists but no multi-turn chat exercised — not scored |
| 9 | Error recovery | **85** (partial) | wrong-type + signed-out + refresh all graceful; network/timeout/429/empty-file not injected |
| 10 | Security | **75** (partial) | signed-out→/login enforced live; cross-tenant + non-admin onboard NOT exercised (needs multi-user run) |
| 11 | Accessibility | **70** (partial) | tab order logical, link/button focus rings present; **text inputs have no visible focus indicator** |
| 12 | Regression | **90** | pytest 1587 green (cited, not re-run); ≈M12 bolt/nut mate-coherence regression LIVE re-verified as passing |

**OVERALL = MIN = 70** (bounded by Performance real latency and Accessibility focus-visibility). Security/Error-recovery/Accessibility are **partial — need dedicated runs** and must not inflate. Conversational N/A (excluded from min).

---

## Findings (schema)

### F1 — W11 identity: revision of an onboarded part does not ground `[major]`
```
persona: P2 sourcing (identity flywheel)  flow: W11  branch: revision re-upload
category: Functional / AI fidelity
observed: Onboarded bracket_A.stl + identity.csv → "Onboarded 1 part · library now 3 · 1
  registered in the declared parts master" (honest). Re-uploaded bracket_A_rev.stl via Verify
  → NO IdentityCard. Line reads "No confident match in your part library yet — geometry
  retrieved 3 prior parts, none confident enough to suggest." Flagship "Looks like your
  Mounting bracket L · PN-BRK-001" suggestion never surfaces; Confirm flow cannot be exercised.
expected: A revision of an onboarded part surfaces "Looks like your <name>" with honest
  confidence + provenance (RETRIEVED · your part library) + caveat, ready to Confirm.
evidence: baseline-shots/w11-03-onboard-readout.png (library 2→3), w11-04-rev-identity.png,
  w11-rev-bodytext.txt ("retrieved 3 prior parts, none confident enough")
failure_reason: retrieval-grounding confidence threshold too strict / geometric signature too
  sensitive to a legitimate revision (bracket_A vs bracket_A_rev). NOTE: the fallback is the
  SAFE, honest one — it never fabricates a match (torus negative control also correctly no-match,
  w11-06). So AI-fidelity honesty is preserved; the FEATURE is just undemonstrable at baseline.
severity: major (flagship new feature's primary use case can't complete end-to-end)
confidence: high (captured value, both positive+negative cases)
recommended_fix: tune identity recall so a true revision clears the suggest threshold while
  keeping the no-fabrication guarantee; add a "closest prior (low confidence)" affordance so the
  retrieved-but-unsure priors are at least offered as a manual pick.
status: open
```

### F2 — Single-part verify + assembly latency `[minor→major UX]`
```
category: Performance
observed: page-load 1.44–1.65s (good). cube.step verify (parse→terminal verdict+cost+preview):
  22.1s cold, 13.8s / 13.3s warm. AS1 assembly: 34s wall (engine self-reports per-part 3.7s for
  18/18 + 32 contact pairs; the rest is GLB/preview + tree).
expected: interactive verify < ~5s for a small part.
evidence: baseline-shots/timing-w1.json, timing-w1b.json, timing-w1c.json (13326–22099 ms);
  w3b-01 header "18/18 costed · 32 contact pairs · 3.7s"
severity: minor (functionally fine, honest spinner "measuring geometry / gates checking in") but
  pervasive — every hero-loop touch pays it.
confidence: high (measured ms). recommended_fix: profile the STEP parse + preview-mesh path;
  warm/pool the mesher; stream partial verdict before preview completes. status: open
```

### F3 — Text inputs lack a visible focus indicator `[minor a11y]`
```
category: Accessibility
observed: On /signup, Tab order is logical (logo→PILOT ACCESS→email→password→Create account→
  Log in). Links & submit button carry outline:auto 1px. The email & password <input>s compute
  outline:"none 0px" and box-shadow:"none" while focused — no visible focus ring (WCAG 2.4.7).
expected: every focusable control shows a visible focus state.
evidence: a11y.mjs TAB_SEQUENCE (inputs outline none/box-shadow none), baseline-shots/a11y-01-signup-focus.png
severity: minor  confidence: medium (computed style; a border-color shift may exist but is not a
  reliable focus affordance)  recommended_fix: add :focus-visible ring to inputs. status: open
```

### F4 — Wrong-type error copy misleads before giving the real reason `[polish]`
```
category: Error recovery / copy
observed: Uploading junk.txt → honest "VERDICT · COULD NOT ANALYZE — This part couldn't be
  tessellated." but the body leads with "The geometry contains a surface our mesher couldn't
  triangulate — Unsupported file type: .txt." A .txt has no geometry/surface; the real reason
  (unsupported type) is correct but buried behind a misleading clause.
expected: lead with the actual reason for a type mismatch.
evidence: baseline-shots/spot-01-wrongtype.png, spot-wrongtype-bodytext.txt
severity: polish (behavior is correct & safe: nothing faked, no cost computed) confidence: high
recommended_fix: branch the copy — type-mismatch vs untriangulable-geometry. status: open
```

### F5 — Driver breakdown reads at a different qty than the headline `[polish]`
```
category: Data fidelity
observed: Headline "Should-cost $8.68/unit on FDM/FFF at qty 10,000"; the driver table sums
  MATERIAL 0.093 + MACHINE 1.406 + LABOR 7.052 + SETUP 0.13 = $8.68, but a note says
  "$8.72 /unit at this qty · band & drivers read at computed qty 100". Two qty bases on one card.
expected: one consistent qty basis, or an explicit "drivers @100 / headline @10k" label.
evidence: baseline-shots/w1-bodytext.txt, w1-02-verdict.png
severity: polish confidence: high recommended_fix: unify or clearly label the qty basis. status: open
```

### Security partial — cross-tenant / non-admin onboard not exercised `[honest gate]`
```
category: Security
observed: signed-out /verify → 302 /login (enforced, spot-02-signedout-verify.png). Backend RBAC
  (analyst-gated /identity/library/onboard, org-scoped reads) exists per SPEC but was NOT probed
  live with a second org / viewer role this run.
status: honest-gate — needs a dedicated multi-user/multi-org run (invite, role PATCH, cross-tenant
  read denial, non-admin onboard 403). Do not score >75 until exercised.
```

---

## What passed with strong evidence

- **W12 Auth** — signup → session cookie → lands on `/verify` Home with a genuinely honest day-zero
  empty state (0 records, 0 machines, n=0 actuals, "Nothing in flight"). Date = Thursday July 9.
  `w12-01-signup-form.png`, `w12-04-verify-home.png`.
- **W1 Verify→Cost** — real 41k-tri tessellated shell of the cube WITH its through-hole (not a
  box/blank), measured bbox 20.0×15.0×10.0mm, vol 2.72cm³, watertight true ● MEASURED. Verdict
  "$8.68/unit on FDM/FFF"; makeability honestly "not evaluated — no machines/world declared". Four
  should-cost drivers all provenance-tagged (○ DEFAULT / ○ MODEL), reconcile to unit cost, "0% of
  drivers grounded"; crossover ~923 units with honest "if redesigned for molding" + draft-DFM
  failure noted; band hatched n=0. `w1-02-verdict.png`, `w1-bodytext.txt`.
- **W2 Environment gate** — toggling sour service (H₂S) triggers a real re-verification; verdict
  flips polymer/$8.68 → steel/CNC-5Axis and materials are struck with cited standard: "Ductile Iron
  excluded … not NACE MR0175 / sour_service", "Mild Steel excluded …", decision recomputed over
  survivors → AISI 4130 (a genuinely NACE-qualifiable low-alloy steel). Technically correct.
  `w2b-03-materials-section.png`, `w2b-after-bodytext.txt`.
- **W3 Assembly** — AS1 = 18 solids / 5 unique designs in baked world positions, real translucent
  render with bolt holes/rods/brackets. Per-part "18/18 costed · 32 contact pairs · 3.7s", FACT
  quantities. Bolt ≈M12 × 37mm BUY $0.75 (×6); nut ≈M12 nut BUY $0.20 (×8) — **bolt & nut AGREE at
  ≈M12** (the historical mate-incoherence regression holds), honest BUY/COTS, aluminum·US DEFAULT
  flagged. `w3b-02-bolt-card.png`, `w3b-03-nut-card.png`, `w3b-nut-bodytext.txt`.
- **W11 onboarding** — Parts screen library-onboard: bracket_A.stl + identity.csv → "library now 3",
  registered in parts master; grid previews all WITHHELD. Negative control torus_unrelated.stl →
  honest no-match. `w11-03-onboard-readout.png`, `w11-06-torus-nomatch.png`. (Positive-match gap = F1.)
- **Reliability** — W1 ×3 identical ($8.68 / FDM / hatched n=0). `w1/w1b/w1c-bodytext.txt`.
- **Error recovery** — wrong-type honest error (F4), signed-out→login, refresh mid-flow recovers to
  `/verify` (session cookie survives). `spot-01/02/03`.

## Top findings ranked

1. **F1 (major)** W11 revision identity doesn't ground — flagship feature undemonstrable end-to-end.
2. **F2 (minor→major)** 13–22s single-part verify / 34s assembly — pervasive UX latency.
3. **F3 (minor)** text inputs have no visible focus ring.
4. **F4 / F5 (polish)** misleading wrong-type copy; dual-qty driver basis.
5. **Security/Error/A11y partials** — need dedicated multi-user + failure-injection + full a11y runs.

## #1 to fix first
**F1 — identity revision grounding.** It is the newest founder-approved capability (parts-master
feeder) and its primary use case (recognize a revision of a part you onboarded) never completes:
the engine retrieves the priors but the confidence threshold withholds the suggestion. The honest
no-fabrication fallback is correct and must be preserved, but recall needs tuning (and/or a
low-confidence "closest prior" manual affordance) so the "Looks like your Mounting bracket L →
Confirm" flow can actually run. Performance (F2) is the close second because it taxes every flow.
