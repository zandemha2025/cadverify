# CadVerify — Human-Sim RE-SCORE Scorecard (after F1–F5 fixes)

- **Date:** 2026-07-10
- **Run type:** Re-score after the five baseline findings (F1–F5) were fixed. Vision-judged, real browser (Playwright/chromium-1194 headless, `--no-sandbox --disable-dev-shm-usage`) driving the live stack at `http://localhost:3042`.
- **Stack:** worktree `/home/user/cadverify/.claude/worktrees/hsim-rescore` (**HEAD c723956** — has commits ca05219 F1, 73802ab F3+F5, e29588e F4, 2c32b2a F2). Backend uvicorn :8042 (`postgres:true`, DB `cadverify_rescore`, all RUNBOOK env incl. `DASHBOARD_SESSION_SECRET`), frontend next dev :3042. **Smoke gate:** `/health` postgres:true; `/auth/signup` → **HTTP 200 + session token** (`{"user":{"id":1,...},"session":"..."}`).
- **Persona:** P1/P5 blend (design engineer + skeptical exec) + P2 (identity flywheel), brand-new empty org (Friday July 10, 0 records, n=0, "Nothing in flight" — honest day-zero).
- **Evidence dir:** `/home/user/cadverify/outputs/human-sim/framework/scorecards/rescore-shots/`
- **Driver:** `rescore-shots/driver.mjs` (phases: auth, a11y, w1/w1b/w1c, w2b, w3b, w11, spot) + `f5probe.mjs`.

---

## Bucket A — PRODUCT (12 categories, 0–100; Overall = MIN)

| # | Category | Baseline | **Rescore** | Δ | Evidence / why it moved |
|---|---|---|---|---|---|
| 1 | Functional correctness | 85 | **95** | +10 | **F1 fixed** — revision-identity now grounds end-to-end: onboard → suggest → **Confirm** → confirmed state; torus still no-match. Everything else correct. |
| 2 | Data fidelity | 90 | **95** | +5 | **F5 fixed** — "What it really takes" driver breakdown (0.093+1.406+7.052+0.13=$8.68) now reconciles to the $8.68/qty-10k headline; the $8.72 is a separately-labeled qty-100 point on the resource-cost curve. Provenance/NACE/FACT-qty intact. |
| 3 | AI fidelity | 90 | **95** | +5 | **F1** positive case now demonstrated honestly: 72% MEDIUM (geometry 93% / name 33%) with explicit "SUGGESTION to confirm, retrieval can be wrong" caveat; torus negative control still "none confident enough" → no fabrication. |
| 4 | Interaction fidelity | 88 | **92** | +4 | Confirm button works; uploads, rail nav, env toggles→re-verify, material switch, part-tree select, onboard pickers all behave. No new defects. |
| 5 | Visual fidelity | 88 | **90** | +2 | Real tessellated renders throughout (cube through-hole, bracket, 18-part assembly x-ray); clean layout; focus ring adds polish. No new visual defects. |
| 6 | **Performance (real ms)** | 70 | **72** | +2 | **F2 partial** — single-part verify **warm 11.8s / 12.6s** (baseline 13.3–13.8s: ~1.5–2s faster, the redundant identity pass removed); **cold still 21.8s** (~baseline 22.1s). Assembly **57.7s this run** (baseline 34s — measured once; likely cold/container variance, flagged). **Still the floor.** |
| 7 | Reliability (3×) | 92 | **92** | 0 | W1 ×3 → identical `$8.68/unit on FDM/FFF`. Deterministic. |
| 8 | Conversational | N/A | **N/A** | — | NL "Ask the engine" surface present; no multi-turn chat exercised. Excluded from MIN. |
| 9 | Error recovery | 85 (partial) | **85** (partial) | 0 | **F4 fixed** (see below) + signed-out→/login + refresh→/verify hold. Still partial: network/timeout/429/empty-file not injected; minor residual "tessellated" toast on .txt. |
| 10 | **Security** | 75 (partial) | **75** (partial) | 0 | signed-out `/verify`→`/login` enforced. **Cross-tenant / non-admin onboard again NOT exercised** (single-user run) — honest cap, needs a multi-user/multi-org run. |
| 11 | Accessibility | 70 (partial) | **85** | +15 | **F3 fixed** — email + password inputs now compute `outline: rgb(107,166,244) solid 2px` while focused (baseline: `none 0px`), visible cobalt ring in screenshot. Full a11y sweep (contrast/all screens) not re-audited → held <100. |
| 12 | Regression | 90 | **90** | 0 | All strong passes hold (render, glass-box cost+provenance, sour NACE gate, ≈M12 bolt/nut mate-coherence). No regression from the 5 fixes. pytest cited green (not re-run). |

### **PRODUCT OVERALL = MIN = 72** (up from **70**).

Still bounded by **Performance (72)** — F2 delivered only a modest warm improvement; cold verify (~22s) and assembly (34–58s) still dominate. **Security (75)** is the next limiter and remains an honest partial (cross-tenant not exercised this run). The four other fixes (F1, F3, F4, F5) moved their categories materially (+4 to +15) but the MIN gate only moves 70→72 because the weakest gate (Performance) barely improved.

---

## The five fixes — did each take? (PASS/FAIL with evidence)

### F1 (flagship) — identity revision grounding — **PASS ✅**
```
persona: P2 identity flywheel   flow: W11   branch: onboard → revision re-verify → confirm → negative control
observed: (1) Onboarded bracket_A.stl + identity.csv → "Onboarded 1 part · library now 2 · 1 registered
  in the declared parts master". (2) Verify bracket_A_rev.stl (real STL render, bbox 80.9×50.6×12.3mm,
  watertight MEASURED) → IdentityCard NOW APPEARS: "PART IDENTITY · RETRIEVED FROM YOUR LIBRARY · ◆ RETRIEVED
  · your part library", "Looks like your Mounting bracket L · PN-BRK-001 · Chassis-2024", "72% · MEDIUM
  CONFIDENCE · geometry 93% · name 33%", caveat "a retrieved SUGGESTION to confirm, not a verified identity;
  retrieval can be wrong — check before trusting", "other near matches: cube.step · 5%", + Confirm button.
  (3) Clicked Confirm → "✓ Identity confirmed — Mounting bracket L · PN-BRK-001 · saved to your part library
  as ● USER — future look-alike parts will carry it" + toast "Identity confirmed — saved to your part library".
  (4) Verify torus_unrelated.stl → STILL "No confident match in your part library yet — geometry retrieved 3
  prior parts, none confident enough to suggest" — NO fabrication.
evidence: rescore-shots/w11-03-onboard-readout.png, w11-04-rev-identity.png, w11-05-confirmed.png,
  w11-06-torus-nomatch.png, w11-rev-bodytext.txt, w11-confirmed-bodytext.txt, w11-torus-bodytext.txt
status: FIXED — the flagship's primary use case now completes end-to-end while preserving the
  no-fabrication guarantee (torus negative control still correctly declines). commit ca05219.
```

### F2 (latency) — single-part verify wall-clock — **PARTIAL ⚠️ (improved, still the floor)**
```
category: Performance
observed: cube.step verify (parse→terminal verdict+cost+preview): cold 21821 ms (baseline 22099 ms cold),
  warm 11807 ms / 12616 ms (baseline 13326–13800 ms warm). ~1.5–2s faster warm — consistent with commit
  2c32b2a "identity signature reuses cost-engine geometry — kill the redundant 2s pass". page-load 1469–1579 ms.
  AS1 assembly 57730 ms this run (baseline 34s; measured once — cold/container variance suspected, needs re-verify).
evidence: rescore-shots/timing-w1.json / timing-w1b.json (implicit) / timing-w3b.json; driver stdout ms.
status: PARTIAL — the redundant identity pass is gone (warm win), but parse+cost still dominate. Cold verify
  (~22s) unchanged; assembly not improved (worse this run). Performance remains the Product MIN. OPEN.
```

### F3 (a11y) — visible input focus ring — **PASS ✅**
```
category: Accessibility
observed: /signup — email input focused computes outline "rgb(107, 166, 244) solid 2px", outlineWidth 2px;
  password input identical. Baseline computed "none 0px" / box-shadow none. Screenshot shows a clear cobalt
  ring around the focused email field.
evidence: rescore-shots/a11y-email-focus-style.json, a11y-pwd-focus-style.json, f3-a11y-email-focus.png
status: FIXED — WCAG 2.4.7 focus indicator now present on the shared AuthField primitive. commit 73802ab.
```

### F4 (error copy) — wrong-type leads with the real reason — **PASS ✅ (minor residual)**
```
category: Error recovery / copy
observed: Uploading junk.txt → main verdict card now leads "VERDICT · COULD NOT ANALYZE" / headline
  "We couldn't read this file." / body "Unsupported file type: .txt. Use .stl, .step, .stp, .iges, or .igs.
  No routing, DFM, or should-cost was computed…". The misleading "mesher couldn't triangulate" clause is GONE
  from the primary copy. Nothing faked, no cost computed.
  Minor: a small transient bottom toast still reads "Could not analyze — this part couldn't be tessellated" —
  secondary/transient, not the primary error.
evidence: rescore-shots/spot-01-wrongtype.png, spot-wrongtype-bodytext.txt
status: FIXED (primary copy) — leads with the real reason. Residual toast is a polish nit. commit e29588e.
```

### F5 (cost qty) — driver breakdown vs headline qty — **PASS ✅**
```
category: Data fidelity
observed: Headline "Should-cost $8.68/unit on FDM/FFF at qty 10,000". "What it really takes" driver breakdown:
  MATERIAL 0.093 + MACHINE TIME 1.406 + LABOR 7.052 + SETUP 0.13 = $8.68 → RECONCILES to the headline qty.
  The $8.72 figure now lives only in the separate "Resource cost" quantity-curve section, explicitly labeled
  "QUANTITY 100 · engine-exact — a computed point" with a slider (crossover ≈923, endpoints 1…10,000) — a
  legitimately different, clearly-labeled view. commit 73802ab introduced makeNowStableEstimate() anchoring the
  Inspector to the make-now route's largest costed qty; baseline read pickEstimate(no qty)=smallest ($8.72@100).
evidence: rescore-shots/f5-drivers-panel.png, f5-driver-panel-text.txt, w1-bodytext.txt
status: FIXED — header, driver breakdown, and Σ-lines all read the same qty ($8.68 @ 10k).
```

---

## Regression re-confirmation (strong passes still hold)

- **Real tessellated render** — cube.step: real 41k-tri shell WITH through-hole, bbox 20.0×15.0×10.0mm, vol 2.72cm³, watertight true ● MEASURED. `w1-02-verdict.png`.
- **Glass-box cost + provenance** — four should-cost drivers all provenance-tagged (○ DEFAULT / ○ MODEL), reconcile to unit cost; crossover ≈923; honest "if redesigned for molding" + draft-DFM failure noted; band hatched n=0. `f5-drivers-panel.png`.
- **Sour gate w/ NACE citations** — Steel + sour service (H₂S): "Ductile Iron excluded … NACE MR0175 / sour_service", "Mild Steel excluded …", decision recomputed over survivors → **CNC 5-Axis · AISI 4130** (NACE-qualifiable). "struck visibly, never dropped silently." `w2b-02-sour-after.png`, `w2b-after-bodytext.txt`.
- **≈M12 bolt/nut mate-coherence** — AS1 = 18 solids in world positions, real assembly shell. bolt **≈M12 × 37mm** BUY $0.75 (×6); nut **≈M12 nut** BUY $0.20 (×8) — **bolt & nut AGREE at ≈M12** (historical ≈M16-into-≈M12 incoherence stays fixed). `w3b-03-nut-card.png`, `w3b-bolt-bodytext.txt`, `w3b-nut-bodytext.txt`.
- **Reliability** — W1 ×3 → identical `$8.68/FDM/FFF`. `w1/w1b/w1c-bodytext.txt`.
- **Auth / day-zero** — signup → session → `/verify` honest empty state (Friday July 10, 0 records, n=0, "Nothing in flight"). `w12-01-signup-form.png`, `w12-04-verify-home.png`.

---

## Remaining findings (ranked)

1. **F2 — Performance (minor→major UX, OPEN).** Cold single-part verify still ~22s; assembly 34–58s. Warm improved ~2s only. This is the Product MIN (72). *Fix:* profile STEP parse + preview-mesh; warm/pool the mesher; stream partial verdict before preview. `timing-*.json`.
2. **Security partial (honest gate, OPEN).** Cross-tenant read denial + non-admin onboard 403 again not exercised (single-user run). *Fix:* dedicated multi-user/multi-org run (invite, role PATCH, cross-tenant denial). Caps Security at 75.
3. **Accessibility full audit (minor, OPEN).** F3 fixed the input focus ring; a full contrast/semantic sweep across all screens not re-run → held at 85.
4. **F4 residual toast (polish).** ".txt couldn't be tessellated" transient toast still shows under the (now-correct) primary error. `spot-01-wrongtype.png`.
5. **Error-recovery injection (partial).** network/timeout/429/empty-file not injected this run → Error recovery held at 85.

---

## Bucket B — ENTERPRISE ATTESTATIONS (separate ledger; NOT in the Product MIN)

Unchanged by this run — each requires a real third-party system this container cannot hold. Reported honestly, never rounded to 100.

| Attestation | In-container status | External input needed to close |
|---|---|---|
| **Live SSO/SCIM cert** | RFC conformance vs a mock IdP proven in-container | Sign-off vs a real Okta/Entra/Ping *tenant* |
| **Live enterprise systems** | Offline integration-run ledger + connector scaffolding present | Real SAP S/4HANA, Windchill/Teamcenter *tenants* |
| **Security posture (SOC 2 / pentest)** | In-container authz/session enforced (that part is Bucket A, must be 100) | SOC 2 audit + independent penetration test |
| **AI / cost accuracy vs ground truth** | Method leakage-safe; product discloses uncertainty (provenance + error bands, `validated=false`) | Real customer quotes/labels to flip `validated=true` — "method proven, number gated" |

---

## Bottom line

- **Bucket A vector:** `[95, 95, 95, 92, 90, 72, 92, N/A, 85, 75, 85, 90]`
- **Product Overall (MIN) = 72** (was 70). Floor = **Performance (72)**; next = **Security (75, unexercised)**.
- **Fixes:** F1 ✅ PASS (flagship, full end-to-end), F2 ⚠️ PARTIAL (warm ~2s faster; still the floor), F3 ✅ PASS, F4 ✅ PASS (minor residual toast), F5 ✅ PASS.
- The four non-latency fixes each raised their category (+4 to +15); the overall MIN moves only 70→72 because it is gated by Performance, which F2 improved only marginally.
