import { ProofShapeClient, createCheckController } from "../../core/src/index.js";
const output=document.querySelector("#output"),button=document.querySelector("#check");
const config=globalThis.PROOFSHAPE_PLUGIN_CONFIG;
const view={progress(message){button.disabled=true;output.innerHTML=`<p>${escapeHtml(message)}</p>`},verdict(v){button.disabled=false;output.innerHTML=`<span class="badge ${v.badge}">${v.badge}</span>${v.issues.length?`<ul>${v.issues.map(x=>`<li>${escapeHtml(x)}</li>`).join("")}</ul>`:"<p>No issues returned.</p>"}${v.sampled?`<p>Sampled: ${escapeHtml(JSON.stringify(v.sampled))}</p>`:""}${v.provenance?`<p>Provenance: ${escapeHtml(JSON.stringify(v.provenance))}</p>`:""}${v.recordUrl?`<p><a target="_blank" rel="noopener" href="${encodeURI(v.recordUrl)}">Open full record</a></p>`:""}`},error(message){button.disabled=false;output.innerHTML=`<span class="badge FAIL">FAIL</span><p>${escapeHtml(message)}</p>`}};
function escapeHtml(x){return String(x).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c])}
if(!config?.apiKey||!config?.baseUrl||typeof globalThis.onshapeExportCurrentPartAsStep!=="function"){button.disabled=true;output.innerHTML="<p>Skeleton only. Configure a user API key and Onshape STEP export adapter during M2.</p>"}else{const controller=createCheckController({client:new ProofShapeClient(config),view,exportStep:globalThis.onshapeExportCurrentPartAsStep});button.addEventListener("click",()=>controller.check())}
