#!/usr/bin/env bash
# ProofShape one-click launcher (macOS). Double-click this file in Finder.
# Requires Docker Desktop: https://www.docker.com/products/docker-desktop/
cd "$(dirname "$0")"
exec bash ./start-proofshape.sh
